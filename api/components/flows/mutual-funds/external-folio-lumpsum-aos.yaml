summary: External Folio - Lumpsum
details:
  - description: As there is no selection required, the buyer app can start the transaction from `init` call also if the seller app supports that. After `on_confirm`, the remaining flow related to payment and on update is the same as regular lumpsum transactions, hence not showing in the flow below.

references: if any
steps:
  - summary: Select folio
    api: select
    details:
      - description: Select additional investment in a particular folio (external)
    reference: if any
    example:
      value:
        $ref: "../../examples/mutual-funds/select/external_folios/select-lumpsum-external-folio-aos.json"
  - summary: On Select
    api: on_select
    details:
      - description: Get the payment options back
    reference: if any
    example:
      value:
        $ref: "../../examples/mutual-funds/on_select/external_folios/on_select-lumpsum-external-folio-aos.json"
